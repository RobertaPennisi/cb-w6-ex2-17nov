/**
 * Project REST API endpoint
 */
const API = "https://edgemony-backend.herokuapp.com/movies";
/**
 * Render some content into an html container
 */
const render = (container, content) => container.innerHTML = content;

export {
    API,
    render
}; 